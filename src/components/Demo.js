import React from 'react';

const Demo = () => (
    <div className="container">
        <p>This demo/boilerplate is setup to use React and React-Router immediately. This will also include Bootstrap 4 CDN so if there's plans to use the modal you'll need to install the React-Bootstrap instead. This will use Bootstrap 3 instead just an fyi.</p>
    </div>
);

export default Demo;